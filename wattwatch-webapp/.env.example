# Copy to .env.local to override locally (optional).
# Local dev defaults are in .env.development (port 5000 = wattwatch-api PORT).
# Production deploy uses .env.production (Render API URL).
#
# Do NOT set VITE_API_URL to localhost when deploying — the live site cannot reach your machine.
VITE_API_URL=http://localhost:5000
